<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Demon Slayer Facts</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand" href="/">
                <span class="decorative-star">✨</span>
                Demon Slayer Facts
                <span class="decorative-star">✨</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/characters">Characters</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/breathing-techniques">Breathing Techniques</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/demons">Demons</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="hero-section">
            <h1 class="hero-title">The Demons! 👹✨</h1>
            <p class="hero-subtitle">
                Learn about the villains from the Demon Slayer world!
            </p>
        </div>

        <div class="row">
            <% if (demons && demons.length > 0) { %>
                <% demons.forEach(function(demon) { %>
                    <div class="col-md-6 col-lg-4">
                        <div class="character-card">
                            <% if (demon.image) { %>
                                <img src="<%= demon.image %>" alt="<%= demon.name %>" onerror="this.src='https://via.placeholder.com/300x250/8b5a8e/ffffff?text=<%= encodeURIComponent(demon.name || 'Demon') %>'">
                            <% } else { %>
                                <img src="https://via.placeholder.com/300x250/8b5a8e/ffffff?text=<%= encodeURIComponent(demon.name || 'Demon') %>" alt="<%= demon.name || 'Demon' %>">
                            <% } %>
                            <div class="card-body">
                                <h3 class="character-name"><%= demon.name || 'Unknown Demon' %></h3>
                                <div class="character-info">
                                    <% if (demon.status) { %>
                                        <p><strong>Status:</strong> <%= demon.status %></p>
                                    <% } %>
                                    <% if (demon.rank) { %>
                                        <p><strong>Rank:</strong> <%= demon.rank %></p>
                                    <% } %>
                                    <% if (demon.gender) { %>
                                        <p><strong>Gender:</strong> <%= demon.gender %></p>
                                    <% } %>
                                    <% if (demon.description) { %>
                                        <p><%= demon.description.substring(0, 100) %><%= demon.description.length > 100 ? '...' : '' %></p>
                                    <% } %>
                                    <% if (demon.abilities && demon.abilities.length > 0) { %>
                                        <p><strong>Abilities:</strong></p>
                                        <% demon.abilities.slice(0, 3).forEach(function(ability) { %>
                                            <span class="badge-custom"><%= ability %></span>
                                        <% }); %>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="col-12">
                    <div class="error-container">
                        <p class="error-message">No demons found! 😢</p>
                        <a href="/" class="btn btn-primary">Go Back Home</a>
                    </div>
                </div>
            <% } %>
        </div>
    </div>

    <footer>
        <p>&copy; 2024 Demon Slayer Facts - Made with 💖 for Demon Slayer Fans!</p>
        <p>Data provided by <a href="https://www.demonslayer-api.com/" target="_blank" style="color: white; text-decoration: underline;">Demon Slayer API</a></p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
